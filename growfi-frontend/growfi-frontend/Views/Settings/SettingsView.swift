import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Настройки")
                .font(.title2).bold()
            Spacer()
        }
        .padding()
    }
} 